<br><br>
<body background="https://ciclovivo.com.br/wp-content/uploads/2016/01/16709401_1417083658336970_568622001_o-696x464.jpg">
<div class="content">
  <div id="login">
    <h2 style="text-align: center;">Log in</h2> </br>
     
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="field">
        <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "Email" %>
      </div>

      <div class="field">
        <%= f.password_field :password, autocomplete: "current-password", placeholder: "Super secret password" %>
      </div>

      <% if devise_mapping.rememberable? %>
        <div class="field">
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me %>
        </div>
      <% end %>

      <div class="actions">
        <%= f.submit "Log in" %>
      </div>
    <% end %>

  <%= render "devise/shared/links" %>
  </div>

</div>

